import React, { memo } from 'react';

const LoginScenery = memo(function LoginScenery() {
  return null; // Keep left pane clean & minimal matching original Image 2 stage
});

export default LoginScenery;
